# Glass-Chat
Supports instant real-time messaging across multiple clients without refresh. Each session gets a unique ID so messages render on opposite sides like real chat. A live typing indicator adds interactivity, while glass blur UI, gradients, hover effects.
