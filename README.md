# Glass-Chat
Here’s a ~350-character version (tight but keeps meaning):  > Supports instant real-time messaging across multiple clients without refresh. Each session gets a unique ID so messages render on opposite sides like real chat. A live typing indicator adds interactivity, while glass blur UI, gradients, hover effects.
